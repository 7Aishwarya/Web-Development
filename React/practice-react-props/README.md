Creating reusable card components and rendering them using props.
- contact cards

props - (keyword in React for properties) used for passing data from one component to another. props are passed in a uni-directional flow. 
